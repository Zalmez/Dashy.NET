# Dashy.NET Environment Configuration
# Copy this file to .env and customize the values

# Database Configuration
# Generate a strong password for production use
DB_PASSWORD=changeme123

# Optional: Custom ports (uncomment to override defaults)
# WEB_PORT=8080
# DB_PORT=5432

# Application Environment (Development/Production)
ASPNETCORE_ENVIRONMENT=Production

# Optional: Custom database settings
# POSTGRES_DB=DashyNet
# POSTGRES_USER=dashynet

# Optional: Custom URLs for the application to bind to
# ASPNETCORE_URLS=http://+:80

# Optional: Logging configuration
# Logging__LogLevel__Default=Information
# Logging__LogLevel__Microsoft.AspNetCore=Warning
